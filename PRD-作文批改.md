# 智慧作业 · 题目答案与批改设置 — 设计文档

> **版本**: v0.2  
> **日期**: 2026-06-10  
> **定位**: 扫描识别二维码后，统一设置本次作业所有题目的答案和批改规则  
> **依赖**: 答题卡制作工具（二维码携带作业配置）

---

## 1. 背景

智慧作业扫描流程中，扫描第一张答题卡 → 识别二维码 → 系统获知本次作业的完整信息（题目数量、题型分布、是否有作文题等）。此时需要一个统一的页面来：

- 为客观题/填空题设置标准答案
- 为作文题设置批改要求
- 所有设置在扫描过程中随时可修改

---

## 2. 触发机制

### 2.1 自动触发

```
扫描第一张答题卡 → 识别二维码 → 获取作业配置
    → 跳转「题目答案与批改设置」页面
    → 老师确认/填写后 → 返回继续扫描
```

### 2.2 手动入口（保留设置入口）

在 `scan-progress.html` 右侧操作面板增加入口按钮，随时可返回修改：

```
scan-progress.html 右侧面板
    ┌─ 扫描操作 ──────────┐
    │ 补扫剩余学生         │
    │ 重新排序              │
    │ 新建扫描任务          │
    ├─ 题目与答案 ─────────┤
    │ 📝 题目答案与批改设置  │  ← 新增入口
    ├─ 数据与记录 ─────────┤
    │ 历史扫描记录          │
    │ 数据分析报告          │
    └──────────────────────┘
```

---

## 3. 页面设计：题目答案与批改设置

### 3.1 整体布局

```
┌─ 题目答案与批改设置 ──────────────────────────────┐
│                                                    │
│  📋 作业：数学周练卷（五）· 高一·1班                  │
│  共 22 题：选择题×10 | 填空题×5 | 解答题×5 | 作文题×1 │
│                                                    │
│  ┌─ 题型 Tab ──────────────────────────────────┐   │
│  │ [选择题(10)] [填空题(5)] [解答题(5)] [作文题(1)] │   │
│  └──────────────────────────────────────────────┘   │
│                                                    │
│  ▼ 当前 Tab 内容区（根据题型切换）                    │
│                                                    │
│  [保存]  [重置为二维码默认值]                         │
└────────────────────────────────────────────────────┘
```

### 3.2 选择题 Tab

```
┌─ 选择题答案 ────────────────────────────────────┐
│                                                  │
│  题号  │  正确答案                                 │
│  ──────┼────────────────────────────────────── │
│   1    │  [A] [B] [C] [D]        ← 单选按钮     │
│   2    │  [A] [B] [C] [D]                       │
│  ...   │  ...                                   │
│  10    │  [A] [B] [C] [D]                       │
│                                                  │
│  每题分值：[ 3 ] 分                               │
└──────────────────────────────────────────────────┘
```

### 3.3 填空题 Tab

```
┌─ 填空题答案 ────────────────────────────────────┐
│                                                  │
│  题号  │  正确答案           │ 允许近义词          │
│  ──────┼───────────────────┼────────────────── │
│   11   │ [ photosynthesis  ] │ [✓] 是            │
│   12   │ [ mitochondria    ] │ [✓] 是            │
│  ...   │ ...                │ ...               │
│                                                  │
│  每题分值：[ 4 ] 分   评分方式：[ 完全匹配 / 模糊匹配 ]│
└──────────────────────────────────────────────────┘
```

### 3.4 解答题 Tab

```
┌─ 解答题答案 ────────────────────────────────────┐
│                                                  │
│  题号  │  参考答案 / 评分要点                      │
│  ──────┼────────────────────────────────────── │
│   16   │ [ 证明：因为 f(x) 在 [0,1] 上连续...   ] │
│        │ 分值：[ 12 ] 分   步骤分：[✓] 启用       │
│   17   │ [ 解：(1) 由题意得...                  ] │
│        │ 分值：[ 14 ] 分   步骤分：[✓] 启用       │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 3.5 作文题 Tab（核心新增）

```
┌─ 作文题批改设置 ────────────────────────────────┐
│                                                  │
│  作文类型：🏴 英语作文（二维码自动识别）            │
│                                                  │
│  📝 作文题目                                      │
│  ┌──────────────────────────────────────────┐   │
│  │ Write an essay about "Artificial          │   │
│  │ Intelligence and Future Education".       │   │
│  │ You should write at least 120 words.      │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  📋 写作要求                                      │
│  ┌──────────────────────────────────────────┐   │
│  │ 1. State your opinion clearly             │   │
│  │ 2. Give at least two supporting reasons   │   │
│  │ 3. Use proper essay structure             │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  🔢 字数要求：[ 120 ] 字                          │
│                                                  │
│  ⚙️ 批改维度                                     │
│  [✓] 错别字/用词不当    [✓] 语法/病句纠正         │
│  [✓] 佳句标注表扬        [✓] 可提升句子点评        │
│  [✓] 综合评分与总评                                │
│                                                  │
│  📊 评分标准（可选）                               │
│  ┌──────────────────────────────────────────┐   │
│  │ 内容与观点 .... 40分                       │   │
│  │ 语言与语法 .... 30分                       │   │
│  │ 结构与逻辑 .... 20分                       │   │
│  │ 书写与卷面 .... 10分                       │   │
│  │ 总计 ........... 100分                     │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  旁批区域：[✓] 答题卡有旁批区（二维码自动检测）     │
└──────────────────────────────────────────────────┘
```

### 3.6 字段说明（作文部分）

| 字段 | 类型 | 必填 | 来源 | 说明 |
|------|------|:---:|------|------|
| 作文类型 | 标签 | 自动 | 二维码 | 英语作文 / 语文作文 |
| 作文题目 | 多行文本 | ✅ | 二维码预填 | 可修改 |
| 写作要求 | 多行文本 | 推荐 | 二维码预填 | 可修改 |
| 字数要求 | 数字 | 推荐 | 二维码预填 | 可修改 |
| 错别字/用词 | 开关 | - | 默认开启 | 定位+纠正 |
| 语法/病句 | 开关 | - | 默认开启 | 纠正+解释 |
| 佳句标注 | 开关 | - | 默认开启 | 标注+表扬 |
| 提升建议 | 开关 | - | 默认开启 | 改写示例 |
| 综合总评 | 开关 | - | 默认开启 | 评分+点评 |
| 评分标准 | 文本 | 可选 | 手动 | 分项分值 |
| 旁批区域 | 标签 | 自动 | 二维码 | 只读，指示打印方式 |

---

## 4. scan-progress 入口

在右侧操作面板「扫描操作」和「数据与记录」之间新增一个分区：

```
┌─ 题目与答案 ──────────┐
│ 📝 题目答案与批改设置   │  ← 点击跳转设置页，随时可修改
└───────────────────────┘
```

首次扫描时如果检测到未设置答案（尤其是作文题），该按钮可加红点提示。

---

## 5. 数据模型

```javascript
HomeworkAnswerConfig {
  homeworkId: string,          // 二维码识别出的作业ID
  homeworkName: string,        // 作业名称
  questionTypes: ['choice','fill','short-answer','essay'],

  // 选择题
  choiceAnswers: [{ index, answer: 'A'|'B'|'C'|'D', score: 3 }],

  // 填空题
  fillAnswers: [{ index, answer: string, allowSynonyms: boolean, score: 4 }],

  // 解答题
  shortAnswers: [{ index, answer: string, stepGrading: boolean, score: 12 }],

  // 作文题
  essayConfig: {
    type: 'english' | 'chinese',
    title: string,
    requirements: string,
    wordCount: number,
    dimensions: { spelling, grammar, highlights, suggestions, overall },
    scoringRubric: string | null,
    hasMarginArea: boolean,    // 是否已预留旁批区
  }
}
```

---

## 6. 页面规划

| 页面 | 说明 |
|------|------|
| `homework-answer-config.html` | **新建**。统一题目答案与批改设置页，按题型Tab切换。扫描首张后自动弹出，scan-progress可随时返回修改 |
| `scan-progress.html` 增强 | 右侧面板增加「题目答案与批改设置」按钮入口 |

---

## 7. 流程总结

```
scan-class-select（选班）→ scan-progress（开始扫描）
    → 扫描第一张 → 识别二维码
        → 跳转 homework-answer-config.html
            ├─ 选择题 Tab：设置答案
            ├─ 填空题 Tab：设置答案+容错
            ├─ 解答题 Tab：设置参考答案+步骤分
            └─ 作文题 Tab：设置题目/要求/批改维度
        → 保存 → 返回 scan-progress
    → 继续扫描后续学生
    → 随时可通过右侧面板「题目答案与批改设置」返回修改
    → 打印旁批（作文）
```
