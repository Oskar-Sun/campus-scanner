# 校园智能扫描打印系统 — 需求规格说明书（PRD）

> **版本**: v1.1  
> **日期**: 2026-07-02  
> **原型文件**: 16 个 HTML 页面（见下方索引）  
> **目标**: 将原型交付研发团队，明确每个页面的功能、交互、数据与边界情况

---

## 目录

1. [系统概述](#1-系统概述)
2. [页面索引与导航拓扑](#2-页面索引与导航拓扑)
3. [全局设计规范](#3-全局设计规范)
4. [页面详细需求](#4-页面详细需求)
   - [4.1 index.html — 首页](#41-indexhtml--首页)
   - [4.2 scan-class-select.html — 智慧作业扫描·选班](#42-scan-class-selecthtml--智慧作业扫描选班)
   - [4.3 designated-scan.html — 指定作业扫描](#43-designated-scanhtml--指定作业扫描)
   - [4.4 scan-progress.html — 扫描进行中](#44-scan-progresshtml--扫描进行中)
   - [4.5 student-preview.html — 学生试卷预览](#45-student-previewhtml--学生试卷预览)
   - [4.6 anomaly-handling.html — 异常处理](#46-anomaly-handlinghtml--异常处理)
   - [4.7 print-settings.html — 打印参数设置](#47-print-settingshtml--打印参数设置)
   - [4.8 scan-history.html — 扫描记录](#48-scan-historyhtml--扫描记录)
   - [4.9 homework-reports.html — 作业报告](#49-homework-reportshtml--作业报告)
5. [数据模型](#5-数据模型)
6. [API 接口需求](#6-api-接口需求)
7. [状态流转图](#7-状态流转图)

---

## 1. 系统概述

**产品定位**: 面向中小学校园的智能扫描打印系统。教师通过连接扫描仪，将学生试卷/答题卡批量扫描，系统自动识别考号、关联学生、批改作业，并生成学情分析报告。

**核心用户**: 学科教师（当前 demo 账号：张建国，高一数学组）

**核心流程**:
```
首页 → 选择扫描模式（智慧作业 / 指定作业）
     → 选择年级/科目/班级
     → 放入试卷 → 扫描仪扫描
     → 逐张识别 → 显示扫描进度
     → [异常处理] → 打印所选学生试卷
     → 查看历史记录 / 作业报告
```

---

## 2. 页面索引与导航拓扑

| 文件 | 页面名称 | 入口来源 |
|------|---------|---------|
| `index.html` | 首页 | 系统入口 |
| `scan-class-select.html` | 智慧作业扫描·选班 | 首页「智慧作业扫描」 |
| `designated-scan.html` | 指定作业扫描 | 首页「指定作业扫描」 |
| `scan-progress.html` | 扫描进行中 | 选班页「开始扫描」、历史记录「查看详情」、报告页「查看详情」 |
| `student-preview.html` | 学生试卷预览 | 扫描页点击已完成学生卡片 |
| `anomaly-handling.html` | 异常处理 | 扫描页点击异常学生卡片、批量处理异常 |
| `print-settings.html` | 打印参数设置 | 扫描页「打印所选学生」 |
| `scan-history.html` | 扫描记录 | 首页「扫描记录」、扫描页「历史扫描记录」 |
| `homework-reports.html` | 作业报告 | 首页「作业报告」、扫描页「数据分析报告」 |

**导航拓扑**:
```
                    ┌──────────────┐
                    │  index.html  │
                    └──────┬───────┘
           ┌───────────────┼───────────────────┐
           ▼               ▼                    ▼
  ┌────────────────┐ ┌──────────────┐ ┌─────────────────┐
  │scan-class-     │ │designated-   │ │scan-history.html│
  │select.html     │ │scan.html     │ └────────┬────────┘
  └───────┬────────┘ └──────┬───────┘          │查看详情
          │                 │                  │
          ▼                 ▼                  ▼
       ┌───────────────────────────────────────────┐
       │           scan-progress.html              │
       │  ┌─────────────┐  ┌───────────────┐       │
       │  │ 点击已扫描   │  │ 点击异常卡片   │       │
       │  │ 学生卡片     │  │ /批量处理异常  │       │
       │  ▼             │  ▼               │       │
       │ student-       │ anomaly-         │       │
       │ preview.html   │ handling.html    │       │
       │               │                  │       │
       │ 打印所选学生   │ 数据分析报告     │       │
       │ ▼             │ ▼               │       │
       │ print-        │ homework-       │       │
       │ settings.html │ reports.html    │       │
       └───────────────────────────────────────────┘
```

---

## 3. 全局设计规范

### 3.1 设计系统（CSS 变量）

| 变量 | 值 | 用途 |
|------|---|------|
| `--accent` | `#00a6ab` | 主色调（青绿色） |
| `--bg-header` | `#006c70` | 页面头部背景 |
| `--bg-primary` | `#f2fafa` | 页面主背景 |
| `--success` | `#10b981` | 成功/已完成 |
| `--danger` | `#ef4444` | 错误/异常 |
| `--warning` | `#f59e0b` | 警告/待处理 |
| `--color-math` | `#00a6ab` | 数学科目色 |
| `--color-english` | `#7c3aed` | 英语科目色 |
| `--color-chinese` | `#f59e0b` | 语文科目色 |
| `--color-physics` | `#10b981` | 物理科目色 |

> **研发注意**: 全局使用这些 CSS 变量，科目色需要支持后台配置扩展。

### 3.2 公共组件

| 组件 | 说明 | 复用页面 |
|------|------|---------|
| `.title-bar` | macOS 风格标题栏，红黄绿三点 + 系统名 | 所有页面 |
| `.header` | 渐变青绿色头部，含返回按钮 + 面包屑 + 页面标题 | 所有页面 |
| `.footer` | 底部操作栏，含提示文字 + 主操作按钮 | 选班页、扫描页 |
| `.btn-primary` | 主操作按钮，青绿渐变 + 光泽叠加 | 多处 |
| `.selection-card` | 可选中卡片，选中时右上角勾选标记 + 发光边框 | 选班页 |
| `.student-card` | 学生信息卡片，左侧科目色条 + 状态图标 | 扫描页 |
| `.ops-btn` | 侧边栏操作按钮，hover 变色 | 扫描页 |
| `.toast` | 顶部居中浮动提示，自动消失 | 全局 |

---

## 4. 页面详细需求

---

### 4.1 index.html — 首页

**路径**: `/` 或 `index.html`  
**原型文件**: [index.html](index.html)

#### 4.1.1 页面结构

```
┌─ title-bar ─────────────────────────────────┐
├─ header（首页，无返回按钮）───────────────────┤
├──────────────────────────────────────────────┤
│  欢迎区域（学校名 + 日期 + 快捷状态卡片）      │
├──────────────────────────────────────────────┤
│  扫描服务                                    │
│  ┌─────────────┐ ┌─────────────┐ ┌────────┐ │
│  │ 智慧作业扫描  │ │ 指定作业扫描  │ │ 扫描记录 │ │
│  │ ✨ 智能识别   │ │ 📋 手动配置   │ │ 📜 历史 │ │
│  └─────────────┘ └─────────────┘ └────────┘ │
├──────────────────────────────────────────────┤
│  数据服务                                    │
│  ┌─────────────────────┐                     │
│  │ 作业报告  📊 数据驱动 │                     │
│  └─────────────────────┘                     │
├─ footer（学校名 + 版本号）───────────────────┤
```

#### 4.1.2 功能卡片详情

**智慧作业扫描** (`scan-class-select.html`)
- 描述: 系统自动识别作业类型、学生考号，智能批改并生成学情报告
- 标签: "✨ 智能识别"
- **行为**: 点击跳转 → `scan-class-select.html`

**指定作业扫描** (`designated-scan.html`)
- 描述: 手动指定作业内容、答案和评分标准，系统按规则批改
- 标签: "📋 手动配置"
- **行为**: 点击跳转 → `designated-scan.html`

**扫描记录** (`scan-history.html`)
- 描述: 查看所有历史扫描任务，支持搜索和筛选
- 标签: "📜 最近记录"
- **行为**: 点击跳转 → `scan-history.html`

**作业报告** (`homework-reports.html`)
- 描述: 基于历史扫描数据自动生成学情分析报告
- 标签: "📊 数据驱动"
- **行为**: 点击跳转 → `homework-reports.html`

#### 4.1.3 数据需求

| 数据 | 来源 | 说明 |
|------|------|------|
| 当前学校名称 | 后台/登录态 | demo: "北京市第一中学" |
| 当前账号 | 后台/登录态 | demo: "张建国" |
| 快捷状态（今日扫描数等） | API | 首页仪表盘数据 |

---

### 4.2 scan-class-select.html — 智慧作业扫描·选班

**路径**: `scan-class-select.html`  
**原型文件**: [scan-class-select.html](scan-class-select.html)

#### 4.2.1 页面结构

```
┌─ title-bar ───────────────────────────────────┐
├─ header（返回首页 | 扫描前选择班级 | 扫描仪选择器 + 驱动设置）─┤
├────────────────────────────────────────────────┤
│  左侧                           │  右侧        │
│  ┌─ 学段：高中（自动识别）─────┐ │ ┌ 扫描设备 ┐ │
│  ├─ ① 选择年级 ───────────────┤ │ │ EPSON... │ │
│  │  [高一] [高二]              │ │ │ ● 已连接 │ │
│  ├─ ② 选择班级（可多选）──────┤ │ ├ 高级设置 ┤ │
│  │  [1班] [2班]               │ │ │ 纸张类型 │ │
│  ├─ ③ 选择科目 ───────────────┤ │ │ 纸张尺寸 │ │
│  │  （仅数学→自动选中）        │ │ │ 亮度等   │ │
│  └────────────────────────────┘ │ └──────────┘ │
├─ footer ───────────────────────────────────────┤
│  已选班级：1班、2班 ｜ 准备就绪    [▶ 开始扫描] │
```

#### 4.2.2 功能需求

**扫描仪选择器**（右上角）
- 下拉列表展示可用扫描仪，每项显示：在线状态（绿点/灰点）+ 设备名 + 状态文字
- 点击在线设备切换当前扫描仪；点击离线设备弹出 toast "该设备当前离线，无法使用"
- 点击外部区域关闭下拉
- **数据来源**: 后台扫描仪设备列表 API

**步骤式选择流程**:
1. **学段**: 自动识别为"高中"（当前学校只有高中学段），以蓝色药丸显示
2. **年级**: 点击后展开班级选择，级联刷新
3. **班级**: 多选（点击切换选中/取消），至少选一个才能下一步
4. **科目**: 当前数据中该老师只教数学，选班后自动选中，跳过手动选择
   > **研发注意**: 如老师教多科（如数学+物理），则需展示科目卡片供选择

**高级设置面板**（可折叠）
- 纸张类型: 常规答题卡 / 红色答题卡 / 环保纸（单选）
- 纸张尺寸: 自动检测 / A3 / A4（单选）
- 亮度/对比度: range 滑块
- **研发注意**: 这些设置需要传递给扫描仪驱动 SDK

**右侧设备信息卡片**
- 实时展示当前选中设备名称和连接状态（绿点脉冲动画表示在线）

**开始扫描按钮**
- 所有选择完成后启用（绿色脉冲动画）
- 点击跳转 → `scan-progress.html?first=1`

#### 4.2.3 交互细节

| 场景 | 行为 |
|------|------|
| 年级切换 | 重置班级和科目选择，隐藏后续步骤 |
| 班级全取消 | 隐藏科目选择，禁用开始按钮 |
| 点击「重新选择」 | 清空所有已选，折叠后续步骤 |
| 按 Escape | 关闭扫描仪下拉菜单 |
| 按 Enter（未选年级时） | 自动选中第一个年级 |

#### 4.2.4 数据模型参考

```javascript
teacherPermissions = {
  stages: ['高中'],                          // 可教学段
  grades: { 'grade-10': { label:'高一', classes:[...], subjects:[...] } },
  classes: { 'class-1': { label:'1班' } },
  subjects: { 'math': { label:'数学' } }
}
```

---

### 4.3 designated-scan.html — 指定作业扫描

**路径**: `designated-scan.html`  
**原型文件**: [designated-scan.html](designated-scan.html)

#### 4.3.1 功能概述

与智慧作业扫描的区别在于：智慧作业由系统自动识别作业类型，指定作业由教师**手动指定**具体要扫描的校本作业本及其章节。

#### 4.3.2 5 步选择流程

| 步骤 | 名称 | 交互 | 数据来源 |
|------|------|------|---------|
| ① | 年级 | 单选卡片 | `teacherPermissions.grades` |
| ② | 科目 | 单选卡片（根据年级动态刷新） | `teacherPermissions.grades[grade].subjects` |
| ③ | 班级 | 多选卡片 | `teacherPermissions.grades[grade].classes` |
| ④ | 校本作业本 | 紧凑单选列表。仅 1 本时自动选中（蓝色药丸），多本时显示列表供选择 | **后台校本配置 API** — 按 `年级+科目` 返回 |
| ⑤ | 章节 | **可搜索滚动列表**。搜索框输入即过滤，右侧显示匹配数。每行含编号圆圈 + 章节名 + 勾选标记 | 选中作业本的 `chapters` 数组 |

#### 4.3.3 关键交互

- **步骤联动**: 上一步完成后才展示下一步，步骤圆点标记完成状态（绿点）
- **作业本自动选中**: 某年级+科目仅 1 本作业本时，不展示选择列表，直接以蓝色药丸显示并自动展开章节
- **章节搜索**: 实时过滤，不区分大小写。计数器显示 `匹配数 / 总节数`
- **章节数量**: 一本作业本可能有 **30~60+** 个章节（如高三一轮复习 55 节），列表 `max-height:260px` 滚动
- **右侧摘要面板**: 实时汇总已选内容（年级→科目→班级→作业本→章节），未选的显示灰色斜体占位

#### 4.3.4 数据模型参考

```javascript
schoolWorkbooks = {
  'grade-10-math': [
    { id:'wb-math-1', name:'高中数学必修第一册·校本作业', chapters:[...28节...] },
    { id:'wb-math-2', name:'高中数学必修第二册·校本作业', chapters:[...24节...] },
  ],
  'grade-12-math': [
    { id:'wb-math-5', name:'高考数学一轮复习·校本作业', chapters:[...55节...] },
  ],
  // ...更多配置
}
```

> **研发注意**: 校本作业本数据应由后台配置管理，学校管理员可维护各年级各科的作业本及其章节。前端按 `grade + subject` 组合键拉取。

---

### 4.4 scan-progress.html — 扫描进行中

**路径**: `scan-progress.html`  
**原型文件**: [scan-progress.html](scan-progress.html)

#### 4.4.1 页面结构

```
┌─ title-bar ─────────────────────────────────────────────┐
├─ header（返回 | 扫描进行中）──────────────────────────────┤
├─ scan-info-bar ─────────────────────────────────────────┤
│  👥 班级 [高一·1班(8/50人)] [高一·2班(2/35人)]           │
│  📋 作业颜色标识：[🟢数学周练卷(五)] [🟣英语单元测试]      │
│  已扫描 15 张 | 已上传 12 张（落后时数字变红）            │
│  ⚠️ 上传落后横幅（仅扫描完成后打印前出现）                │
├──────────────────────────────────────────────────────────┤
│  左侧卡片区                    │  右侧操作面板             │
│  ┌ 已扫描识别 15人 ──────────┐ │  扫描操作                │
│  │ [颠倒顺序] [全选]          │ │  [补扫剩余学生] 🔒       │
│  │ ✓张三 ✓李四 ✓王五 ⚠孙八   │ │  [重新排序] [?] 🔒      │
│  │ ...（学生卡片网格，8列）    │ │  [新建扫描任务] 🔒       │
│  ├ 未扫描学生 20人 ──────────┤ │  答案与评分要求          │
│  │ ○黄同学 ○林同学 ...       │ │  [设置答案]              │
│  └───────────────────────────┘ │  数据与记录              │
│                                │  [历史扫描记录]          │
│                                │  [数据分析报告]          │
├─ footer ─────────────────────────────────────────────────┤
│  已选择 N 份 | 已扫描 N/M 人 | 已上传 N 人 | 已打印 N 人  │
│  [批量处理异常 N] [打印所选学生] 🔒（扫描中禁用）          │
```

#### 4.4.2 核心架构：扫描 → 上传 → 识别 三阶段分离

**设计决策**：卡片只展示服务端已接收的数据。客户端扫描动作仅体现在计数上，卡片在服务端返回数据后才出现。

| 阶段 | 触发方 | 页面表现 |
|------|--------|---------|
| ① 扫描 | 扫描仪（客户端） | `已扫描 +1`，无卡片出现 |
| ② 上传 | 服务端接收 | `已上传 +1`，卡片出现，状态="识别中" |
| ③ 识别 | 服务端处理 | 卡片进度 0%→30%→70%→完成 |

**并行处理**：纸张连续进纸（间隔 600ms），每张纸独立走 上传→识别 链路，互不阻塞。多张卡片同时处于不同阶段。

#### 4.4.3 功能需求

**扫描/上传计数**（scan-info-bar 右侧）
- `已扫描 X 张`：扫描仪已过纸张数（客户端实时更新）
- `已上传 Y 张`：服务端已接收数
- 当 X > Y 时，"已上传"数字**变红**醒目提示
- 不显示持续闪烁的"落后/上传中"角标，避免干扰
- **研发注意**: 已扫描数由扫描仪 SDK 事件驱动；已上传数由服务端 WebSocket 推送

**学生卡片**（左侧主体区域）
- **卡片不立即出现**：扫描完成后仅计数，卡片等服务端返回数据后才渲染
- **卡片状态只有两种**:
  - `识别中`：旋转 spinner + "识别中 X%"
  - `done`：勾选框 + 姓名 + 异常标签（如有）
- **没有"上传中"状态**（该动作在服务端侧完成）
- **左侧 3px 色条**：表示所属作业（数学=青绿、英语=紫色等）
- **已打印角标**：卡片右侧竖排显示"已打"（灰色低对比度，不占水平空间）
- **异常卡片**:
  - `考号关联异常` / `扫描异常`：隐藏学生姓名（身份未识别），以 `flex:1` 占位符保持标签排列
  - `重复扫描` / `二维码异常`：显示学生姓名
  - 异常类型标签使用缩写（如"考号异常"），完整描述通过 `title` 悬停展示
- **所有卡片默认选中**（含异常卡片，未打印前均选中）
- 点击正常 done 卡片 → 跳转 `student-preview.html`
- 点击异常卡片主体 → 切换选中（支持打印）
- 点击异常标签 → 跳转 `anomaly-handling.html`
- **未扫描区域**：灰色虚线边框卡片，不可点击

**颠倒顺序按钮**
- 所有卡片识别完成后才启用
- 点击将 `scanStates` 数组反转

**扫描中按钮锁定**
- 扫描进行中时，以下按钮**全部不可点击**：
  - 返回按钮（header，pointer-events: none + 半透明）
  - 补扫剩余学生、重新排序、新建扫描任务（ops-btn--disabled）
  - 打印所选学生、批量处理异常（disabled）
- 判断"扫描中"的条件（任一满足即锁定）：
  - `scanTimer` 活跃（还有卡片等待进纸）
  - `isContinueScanning === true`
  - `pendingScanCount > 0`（有纸张正在上传）
  - 有卡片状态为 `recognizing`

**打印流程**（两步式）
1. scan-progress 点击「打印所选学生」→ 存储选中学生姓名到 `sessionStorage` → 跳转 `print-settings.html`
2. print-settings 配置参数后点击「确认打印」→ 设 `sessionStorage.pendingPrint = true` → 跳回 scan-progress
3. scan-progress 检测到 `pendingPrint`，按姓名匹配标记卡片为已打印，取消选中，显示 toast
- **上传不一致时**：点击打印弹出 confirm 强提示"请确认已将未上传试卷从打印机中取走"

**补扫剩余学生**
- 从 `unscannedStates` 取最多 5 人，并行进纸扫描
- 同样走 扫描→上传→识别 流程

#### 4.4.4 扫描模拟逻辑（原型）

> **研发注意**: 原型中使用 setTimeout 模拟，实际需对接扫描仪 SDK + 服务端 WebSocket。

```
并行处理时间线（以 3 张纸为例）:
  t=0ms:    扫描纸1 → scannedCount=1
  t=600ms:  扫描纸2 → scannedCount=2
  t=900ms:  上传纸1 → 卡片1出现(识别中 0%)
  t=1200ms: 扫描纸3 → scannedCount=3
  t=1350ms: 识别纸1 30%
  t=1500ms: 上传纸2 → 卡片2出现(识别中 0%)
  t=1800ms: 识别纸1 70%
  t=2100ms: 上传纸3 → 卡片3出现(识别中 0%)
  t=2250ms: 识别纸1 → done ✓
  ...

单张卡片时间: 进纸间隔600ms → 上传900ms → 识别1350ms → done
```

#### 4.4.5 状态变量

| 变量 | 类型 | 说明 |
|------|------|------|
| `scanStates` | Array | 已上传识别的学生卡片列表 |
| `unscannedStates` | Array | 未扫描学生列表 |
| `scannedCount` | Number | 扫描仪已过纸张数（客户端计数） |
| `uploadedCount` | Number | 服务端已接收数 |
| `pendingScanCount` | Number | 已扫描但尚未上传的数量 |
| `scanTimer` | Interval\|null | 并行扫描定时器 |
| `isContinueScanning` | Boolean | 是否正在补扫 |
| `continueQueue` | Array | 补扫队列 |
| `studentQueue` | Array | 初始扫描队列（40 人） |

---

### 4.5 student-preview.html — 学生试卷预览

**路径**: `student-preview.html`  
**原型文件**: [student-preview.html](student-preview.html)

#### 4.5.1 功能概述

教师点击已完成扫描的学生卡片后，进入该页查看试卷扫描图像，支持缩放、旋转、翻页等操作。

#### 4.5.2 功能需求

- **试卷图像展示**: 居中显示扫描图像，支持鼠标滚轮缩放、拖拽平移
- **缩略图导航**: 底部或侧边显示所有页面缩略图，点击切换
- **学生切换**: 上一个/下一个按钮，在已扫描学生列表中导航
- **工具栏**: 缩放（+/-/适应页面）、旋转（顺时针/逆时针90°）、下载、打印
- **信息栏**: 显示学生姓名、考号、作业名称、扫描时间
- **返回**: 返回 `scan-progress.html`

---

### 4.6 anomaly-handling.html — 异常处理

**路径**: `anomaly-handling.html`  
**原型文件**: [anomaly-handling.html](anomaly-handling.html)

#### 4.6.1 功能概述

处理扫描过程中产生的异常试卷。按异常类型分为 4 个 Tab：扫描异常、二维码异常、考号关联异常、重复扫描。

#### 4.6.2 功能需求

- **异常类型 Tab 切换**: 4 个 Tab，每个显示该类型异常数量
- **异常列表**: 左侧显示异常试卷缩略图列表，当前选中高亮
- **图像预览区**: 中间显示原始扫描图像
- **操作面板**（右侧）:
  - 学生信息: 显示识别到的考号、推测学生名
  - 处理方式: 重新识别 / 手动录入考号 / 跳过此页 / 重新扫描
  - 关联学生: 搜索并选择正确的学生进行关联
- **上下导航**: 在同类型异常间切换
- **底部状态**: 当前处理进度 (N/M)

#### 4.6.3 数据模型参考

```javascript
anomalyTypeToKey = {
  '扫描异常': 'scan',
  '二维码异常': 'qrcode',
  '考号关联异常': 'examNo',
  '重复扫描': 'duplicate',
}
```

---

### 4.7 print-settings.html — 打印参数设置

**路径**: `print-settings.html`  
**原型文件**: [print-settings.html](print-settings.html)

#### 4.7.1 功能概述

教师在扫描页选择学生后，进入此页配置打印参数并发起打印。

#### 4.7.2 功能需求

**打印设置分组**:
| 设置组 | 选项 | 默认值 |
|--------|------|--------|
| 打印机选择 | 下拉列表（已连接打印机） | 系统默认 |
| 纸张尺寸 | A4 / A3 / 自动 | A4 |
| 打印份数 | 数字输入 | 1 |
| 双面打印 | 开关 | 关闭 |
| 色彩模式 | 彩色 / 黑白 | 黑白 |
| 缩放模式 | 实际大小 / 适应页面 / 自定义% | 适应页面 |
| 打印范围 | 全部 / 仅成绩页 / 自定义页数 | 全部 |
| 水印 | 开关 + 水印文字输入 | 关闭 |

**学生列表**: 展示待打印学生姓名、考号、作业名称
**打印预览**: 缩略图预览
**底部**: 「取消」+「开始打印」按钮

**驱动设置按钮**: 打开系统打印机驱动设置对话框

---

### 4.8 scan-history.html — 扫描记录

**路径**: `scan-history.html`  
**原型文件**: [scan-history.html](scan-history.html)

#### 4.8.1 功能需求

**筛选栏**:
- 搜索框: 按作业名称/班级/人员搜索
- 类型筛选: 全部记录 / 智慧作业 / 指定作业
- 日期范围: 开始日期 ~ 结束日期，支持清除

**数据表格**:

| 列名 | 说明 |
|------|------|
| 扫描时间 | 格式 `YYYY/MM/DD HH:mm` |
| 扫描类型 | 智慧作业（青绿标签）/ 指定作业（紫色标签） |
| 作业名称 | 彩色圆点 + 作业名 + 张数。多个作业用 `·` 分隔 |
| 操作人员 | 教师姓名 |
| 班级 | 如"高一·1班" |
| 人数 | 如"28 人" |
| 操作 | 「查看详情」按钮 |

**交互**:
- 点击行或「查看详情」→ 跳转 `scan-progress.html?id=记录ID`
- 分页: 每页 15 条，页码按钮，支持键盘 ← → 翻页
- 搜索/筛选即时生效，页码重置为第 1 页

#### 4.8.2 数据需求

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 记录唯一 ID |
| `time` | datetime | 扫描时间 |
| `type` | enum | `smart` / `designated` |
| `assignments` | array | `[{name, count}]` 一次可扫多个作业 |
| `user` | string | 操作教师 |
| `class` | string | 班级名 |
| `count` | int | 总扫描人数 |

---

### 4.9 homework-reports.html — 作业报告

**路径**: `homework-reports.html`  
**原型文件**: [homework-reports.html](homework-reports.html)

#### 4.9.1 功能概述

以手风琴卡片展示每次作业的学情报告。每个卡片代表一次作业，默认展开第一个。

#### 4.9.2 功能需求

**摘要栏**（顶部）:
- 本学期作业 N 次 | 已完成批改 N 次 | 待批改 N 次

**报告卡片（收起状态）**:
- 左侧科目色条 + 科目图标（∑/En/文/φ）
- 作业名称、科目、扫描时间、涉及班级数
- 三项统计数字：总人数 / 已提交 / 已批改
- 「查看详情」按钮 → 跳转 `scan-progress.html`

**报告卡片（展开状态）**:
- 每个班级一行，包含：
  - 班级名称
  - 提交进度条 + 百分比（绿≥90% / 黄≥70% / 红<70%）
  - 批改进度条 + 百分比
  - 批改状态标签（✓ 已批改 / 批改中 / 待批改）
  - 未交学生名单（红色标签）：`⚠ 未交 N 人：张三、李四…`，hover 展开完整名单

**交互**:
- 点击卡片头部展开/收起（手风琴模式，同一时间只有一个展开）
- 首次加载默认展开第一个卡片
- 「查看详情」按钮阻止事件冒泡（不触发展开/收起）

#### 4.9.3 数据需求

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 报告 ID |
| `name` | string | 作业名称 |
| `subject` | enum | 科目（数学/英语/语文/物理） |
| `time` | datetime | 扫描时间 |
| `classes` | array | 涉及班级列表 |
| `classData` | array | 每班详情 `{name, total, submitted, corrected, pending, unsubmitted, unsubNames}` |

---

## 5. 数据模型

### 5.1 核心实体

```
Teacher（教师）
  ├─ id, name, schoolId
  ├─ permissions: { stages, grades, classes, subjects }
  └─ currentDevice: Scanner

Student（学生）
  ├─ id, name, examNo（考号）
  ├─ classId, gradeId
  ├─ printed: boolean（是否已打印）
  └─ status: 'unscanned' | 'recognizing' | 'done'（注：无 'uploading'，上传由服务端处理）

ScanTask（扫描任务）
  ├─ id, type: 'smart' | 'designated' | 'free'
  ├─ teacherId, classIds, subjectId
  ├─ assignments: [{ name, count }]（智慧作业可多个，指定/自由作业仅 1 个）
  ├─ workbookId?, chapterId?（指定作业）
  ├─ scannedCount, uploadedCount, anomalyCount
  └─ status: 'scanning' | 'completed' | 'cancelled'

Anomaly（异常记录）
  ├─ id, scanTaskId, studentId
  ├─ type: 'scan' | 'qrcode' | 'examNo' | 'duplicate'
  ├─ status: 'pending' | 'resolved'
  └─ resolveAction: 'retry' | 'manual' | 'skip' | 'rescan'

Workbook（校本作业本）
  ├─ id, name, gradeId, subjectId
  └─ chapters: [{ order, name }]

HomeworkReport（作业报告）
  ├─ id, scanTaskId
  ├─ classData: [{ className, total, submitted, corrected, unsubmitted, unsubNames }]
  └─ generatedAt
```

### 5.2 科目-颜色映射

| 科目 | CSS 变量 | 色值 | 背景色 |
|------|---------|------|--------|
| 数学 | `--color-math` | `#00a6ab` | `#e0f7f8` |
| 英语 | `--color-english` | `#7c3aed` | `#ede9fe` |
| 语文 | `--color-chinese` | `#f59e0b` | `#fef3c7` |
| 物理 | `--color-physics` | `#10b981` | `#d1fae5` |

> **研发注意**: 此映射需支持后台动态配置，前端通过 API 获取科目列表及对应颜色。

---

## 6. API 接口需求

| 接口 | 方法 | 说明 | 使用页面 |
|------|------|------|---------|
| `/api/teacher/profile` | GET | 获取教师信息及权限 | 全局 |
| `/api/teacher/permissions` | GET | 获取教师的年级/班级/科目权限 | 选班页 |
| `/api/devices` | GET | 获取可用扫描仪/打印机列表 | 选班页、打印页 |
| `/api/workbooks?grade=X&subject=Y` | GET | 获取校本作业本列表 | 指定作业扫描 |
| `/api/workbooks/:id/chapters` | GET | 获取作业本章节列表 | 指定作业扫描 |
| `/api/scan/start` | POST | 发起扫描任务 | 选班页 |
| `/api/scan/progress/:taskId` | GET | 获取扫描进度（轮询/WebSocket） | 扫描页 |
| `/api/scan/students/:taskId` | GET | 获取已扫描学生列表 | 扫描页 |
| `/api/scan/anomalies/:taskId` | GET | 获取异常列表 | 异常处理页 |
| `/api/scan/anomalies/:id/resolve` | POST | 处理异常 | 异常处理页 |
| `/api/scan/continue` | POST | 继续扫描（补扫） | 扫描页 |
| `/api/scan/history` | GET | 扫描记录列表（分页+筛选） | 历史页 |
| `/api/scan/history/:id` | GET | 扫描记录详情 | 历史页→扫描页 |
| `/api/print/settings` | POST | 发起打印 | 打印页 |
| `/api/reports` | GET | 作业报告列表 | 报告页 |
| `/api/reports/:id` | GET | 作业报告详情 | 报告页 |
| `/api/student/:id/preview` | GET | 学生试卷图像 | 预览页 |

> **研发注意**: 扫描进度建议使用 WebSocket 推送，避免轮询对服务器造成压力。

---

## 7. 状态流转图

### 7.1 扫描任务状态

```
[用户选择班级] → [放入试卷] → [扫描仪开始扫描]
     │                              │
     ▼                              ▼
  PENDING ────────────────────→ SCANNING（客户端计数 +1，无卡片）
                                    │
                                    ▼（服务端接收）
                               RECOGNIZING（卡片出现，识别中）
                                    │
                          ┌─────────┼─────────┐
                          ▼         ▼         ▼
                      正常识别   识别异常    DONE
                          │         │         │
                          ▼         ▼         ▼
                       DONE     异常标签     [打印]
                          │         │         │
                          └────┬────┘         ▼
                               ▼           PRINTED
                           [处理完成] → COMPLETED
```

### 7.2 学生卡片状态

```
纸张过机 → 服务端接收 → 识别中(recognizing) → done
  │           │              │                  │
  │      scannedCount++  spinner+进度%      ┌───┴───┐
  │      uploadedCount++                  正常     异常
  │                                      ✓选中    ⚠+异常标签
  │                                                ✓选中
  └── 卡片不出现，仅计数 ──────────────────────────→（打印前全部默认选中）
                                                      │
                                                  [打印后]
                                                      ▼
                                                 PRINTED（取消选中，右侧竖排"已打"）
```

---

## 附录

### A. 原型文件清单

| 文件 | 行数 | 大小 |
|------|------|------|
| index.html | ~380 | ~12KB |
| scan-class-select.html | ~1210 | ~40KB |
| designated-scan.html | ~700 | ~22KB |
| scan-progress.html | ~1610 | ~52KB |
| student-preview.html | ~550 | ~18KB |
| anomaly-handling.html | ~650 | ~22KB |
| print-settings.html | ~400 | ~13KB |
| scan-history.html | ~490 | ~16KB |
| homework-reports.html | ~400 | ~13KB |

### B. 浏览器兼容性

- 目标浏览器: Chrome 90+, Edge 90+, Safari 15+
- 不要求支持 IE
- 使用 CSS 变量、Flexbox、Grid，无第三方依赖

### C. 后续迭代建议

1. 接入真实扫描仪 SDK（如 TWAIN/WIA/SANE）
2. 接入 OCR 考号识别服务
3. 接入 AI 批改引擎（客观题自动批改，主观题辅助批改）
4. 增加多教师协作批改功能
5. 增加家长端查看报告功能
6. 支持移动端（PWA）
