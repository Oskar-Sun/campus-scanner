# 页面生成 · 颜色与样式约束提示词

> 用途：把本段整体粘贴给页面生成 AI，作为**不可违背的视觉约束层**，再附上你的具体页面需求。

---

## 0. 铁律（最高优先级）

1. **颜色只能取自下方调色板**，禁止任何表外的 hex / rgb / hsl / 具名色。
2. **一律引用 CSS 变量名**（如 `var(--color-primary-700)`），不得写死色值。
3. 需要半透明时**只用 overlay token**，禁止对实色自行加 opacity / rgba 调色。
4. **专色专用**：语义色（error/warning/info/success）只表达其语义，不做装饰。
5. 冲突时，本约束 > 你的审美 > 默认风格。

---

## 1. 设计基调（按参考风格固定）

- 浅色主题：白卡片 + 浅灰底分层，留白充足。
- 青绿（teal）为品牌主色，仅用于品牌与交互，不滥铺。
- 卡片化布局，大圆角、细分隔线、徽章化状态。
- 顶部为白色导航条；其下可有一段青绿渐变 banner 作视觉锚点。
- 信息密度中等：关键数字用主色高亮，状态用浅底+深字标签表达。

---

## 2. 调色板（唯一合法颜色）

**主色 primary（品牌/交互）**
50 `#effafa` · 100 `#def8f9` · 200 `#b2f1f2` · 300 `#7de3e6` · 500 `#50d8dc` · 700 `#00a6ab` · 950 `#007a7e`

**中性 gray**
50 `#f8f8fc` · 100 `#d2d3de` · 200 `#bcbdc0` · 300 `#a2a3a6` · 400 `#88898c` · 500 `#6e6f71` · 600 `#545557` · 700 `#3a3b3d` · 900 `#212123`

**基础 base**
01 `#000000`（纯黑，慎用） · 02 `#ffffff`

**语义 semantic**
- success（青绿）：100 `#bef8fa` · 300 `#66dade` · 500 `#11c5ca` · 700 `#00a6ab` · 900 `#008a8f`
- error（红）：100 `#ffece8` · 300 `#fdcdc5` · 500 `#fbaca3` · 700 `#f53f3f` · 900 `#cb2634`
- warning（橙）：100 `#fff7e8` · 300 `#ffe4ba` · 500 `#ffcf8b` · 700 `#ff7d00` · 900 `#d25f00`
- info（蓝）：100 `#e4f0fb` · 300 `#97c4f1` · 500 `#3089e3` · 700 `#2675c4` · 900 `#185695`

**强调 accent（分类/图表专用，成对：浅底 50/400 ＋ 强调 600/800）**
- accent-1 绿（near，与主色协调）：50 `#d2f0e1` · 400 `#a7eaca` · 600 `#5ad0a3` · 800 `#049d75`
- accent-2 粉：50 `#fff1f7` · 400 `#fbcde4` · 600 `#ff86c8` · 800 `#d14692`
- accent-3 紫：50 `#f3f1ff` · 400 `#cfc6ff` · 600 `#8b6ff2` · 800 `#5936b1`
- accent-4 黄：50 `#fff9e7` · 400 `#ffeeb6` · 600 `#f9b932` · 800 `#c27b00`

**遮罩 overlay（半透明唯一来源）**
- alpha-2（黑底）：`rgba(19,19,21, .05/.1/.2/…/.9)` → 弹窗遮罩、深色叠层
- alpha-1（白底）：`rgba(255,255,255, .05/.1/.2/…/.9)` → 玻璃/高光叠层

---

## 3. 颜色角色映射（强约束：什么颜色用在什么地方）

| 角色 | Token | 说明 |
|---|---|---|
| 页面底色 | `gray-50` | 最外层背景 |
| 卡片 / 导航条 / 弹窗底 | `base-02` | 内容容器 |
| 品牌主色 | `primary-700` | logo、主按钮填充、选中态、关键数字、链接 |
| 主色 hover / 渐变亮端 | `primary-500` | 悬停、banner 渐变亮侧 |
| 主色按下 / 渐变暗端 | `primary-950` | 点按态、banner 渐变暗侧 |
| 主色浅底 | `primary-50` / `100` | 选中行底、主色标签底、hover 底 |
| 正文 / 标题 | `gray-900` | 主文字（**替代纯黑**） |
| 次要文字 | `gray-500` | 辅助说明、占位 |
| 失效文字 | `gray-400` | disabled |
| 描边 / 分隔线 / 次按钮边 | `gray-100` | 卡片边框、divider、ghost 按钮描边 |
| 中性标签底 | `gray-50` | 「共13题」「选择题」「极难」类信息标签 |
| 中性标签字 | `gray-500`～`700` | 同上 |
| 错误 / 错题计数 / 危险操作 | 底 `error-100` ＋ 字 `error-700` | 「错2」、删除、报错 |
| 警示 / 收藏 / 待办 | `warning-700`；底 `warning-100` | 收藏图标、提醒条 |
| 中性信息提示 | 字 `info-700` ＋ 底 `info-100` | 引导文案、说明气泡 |
| 成功 / 通过状态 | 字 `success-700` ＋ 底 `success-100` | 提交成功、达标（**仅状态，非交互**） |
| 分类色 / 图表系列 | `accent-1~4` | 数据可视化、标签归类（**禁止用于按钮/链接**） |
| 弹窗遮罩 | `overlay-alpha-2-*` | 模态背景 |
| 玻璃 / 高光叠层 | `overlay-alpha-1-*` | banner 上的浅色叠层 |

---

## 4. 关键区分规则（最易被 AI 弄错，重点遵守）

1. **primary ≠ success（两者都是青绿）**
   `primary` = 品牌与交互（可点击、选中、主按钮）；`success` = 结果反馈（成功/通过）。
   → 不要用 `success` 做按钮，也不要用 `primary` 表示「成功状态」。

2. **accent 只做分类，不做交互**
   `accent-1~4` 仅用于图表系列色、标签分类、数据可视化。
   按钮、链接、主操作一律走 `primary`。
   配对用法：`*-50/400` 作柔和底，`*-600/800` 作强调字/描边。

3. **灰阶分工固定**
   `gray-900` 文字 / `gray-500` 次要 / `gray-400` 失效 / `gray-100` 描边分隔 / `gray-50` 容器底。
   `gray-200/300/600/700` 仅按需插值，**不得新增灰**。

4. **透明度只走 overlay**
   要半透明黑→`overlay-alpha-2-*`；要半透明白→`overlay-alpha-1-*`。禁止对实色加 opacity。

5. **渐变只用 primary 区间**
   banner / 强调渐变限 `primary-950 → primary-700 → primary-500`，禁止跨色系渐变。

---

## 5. 形态约束（次要，让风格贴近参考图）

- **圆角**：卡片/输入框 `8–10`（soft/round）；按钮/标签胶囊 `full(9999)`；大容器/banner `28`（large）；最小元素 `6`（small）。
- **间距**：用 8 基准的档位 `8 / 12 / 16 / 24 / 32 / 40`，不要随意取值。
- **字号**：caption `11–12` · body `13–15` · subheading `16–24` · heading `28–36` · display `40–48`。
- **描边**：常规 `1px`，强调 `1.5px`，发丝线 `0.5px`。

---

## 6. 输出前自检清单（生成 AI 必须逐条核对）

- [ ] 全部颜色都是 `var(--color-*)`，无硬编码、无表外色？
- [ ] 半透明是否只来自 overlay token？
- [ ] 按钮/链接是否只用 primary，未误用 accent 或 success？
- [ ] error/warning/info/success 是否只出现在对应语义处？
- [ ] 大面积文字用 `gray-900` 而非 `#000`？
- [ ] 渐变是否只在 primary 区间内？
- [ ] 圆角/间距/字号是否落在上面的档位上？
